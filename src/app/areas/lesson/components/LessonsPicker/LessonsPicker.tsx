import "./LessonsPicker.scss"

import { ReactNode } from "react"

interface LessonsPickerProps {
  children: ReactNode
}

function LessonsPicker(props: LessonsPickerProps) {
  return (
    <div className="lessons-picker">

    </div>
  )
}

export default LessonsPicker
